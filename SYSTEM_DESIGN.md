# AI-Teacher 시스템 설계 문서

> 작성일: 2026-03-23
> 버전: v0.1 (초안)

---

## 1. 프로젝트 개요

### 배경

온택트 교실은 교사 1명이 온라인으로 다수의 학생을 개별 관리하는 방식이다. 교사는 학생 방을 순차적으로 방문하기 때문에, 학생 입장에서는 교사 부재 시간이 발생한다. 이 공백을 AI 선생님이 채워 학생이 수업 내용을 복습하고 정리하도록 돕는다.

### 핵심 흐름

```
교사 퇴장 → AI 선생님 등장 → 학생과 대화(복습) → 대화 요약 → 교사에게 전송 → AI 종료
```

### 목표

- 학생이 수업 내용을 자신의 말로 설명하며 능동적으로 복습
- 교사는 자리를 비운 동안의 학습 내용을 요약본으로 파악
- 단계적 고도화를 통해 기술 리스크 최소화

---

## 2. 단계별 시스템 설계

### Stage 1 — 녹음 후 요약 전송 (Baseline)

가장 단순한 형태. 학생이 수업 내용을 혼자 녹음하면 서버가 처리 후 요약을 교사에게 전달한다.

#### 흐름도

```
[학생]
  ↓ 녹음 버튼 클릭
[브라우저 - MediaRecorder API]
  ↓ 오디오 파일(webm/mp3) 업로드
[백엔드 서버]
  ↓ STT 변환 요청
[OpenAI Whisper API]
  ↓ 텍스트 반환
[백엔드 서버]
  ↓ 요약 요청
[Claude API (claude-sonnet-4-6)]
  ↓ 요약 텍스트 반환
[백엔드 서버]
  ↓ 저장 + 알림 발송
[PostgreSQL DB] + [교사 대시보드 / 이메일]
```

#### 기술 스택

| 역할 | 기술 | 선택 이유 |
|------|------|----------|
| 프론트엔드 | Next.js 15 (App Router) | 빠른 개발, SSR 지원 |
| 오디오 녹음 | Web MediaRecorder API | 브라우저 기본 지원, 별도 설치 불필요 |
| 백엔드 | FastAPI (Python) | 비동기 처리, AI 라이브러리 호환성 |
| STT | OpenAI Whisper API | 한국어 인식률 우수, 간단한 통합 |
| 요약/LLM | Claude API (claude-sonnet-4-6) | 한국어 이해력, 긴 컨텍스트 지원 |
| DB | PostgreSQL | 대화 이력, 세션 관리 |
| 파일 저장 | AWS S3 or 로컬 | 오디오 원본 보관 |
| 교사 알림 | 웹 대시보드 + 이메일(SendGrid) | 실시간 확인 가능 |

#### 데이터 모델

```
Session
  ├── session_id (UUID)
  ├── student_id
  ├── teacher_id
  ├── started_at
  ├── ended_at
  ├── audio_url
  ├── transcript (text)
  └── summary (text)
```

---

### Stage 2 — 실시간 STT + 요약 전송

학생이 말하는 즉시 텍스트로 변환되며, 세션 종료 시 요약이 전송된다. 교사는 실시간으로 텍스트를 모니터링할 수 있다.

#### 흐름도

```
[학생 마이크]
  ↓ 오디오 스트림 (WebSocket)
[백엔드 서버]
  ↓ 청크 단위 전송
[Deepgram Streaming STT]
  ↓ 실시간 텍스트 반환 (WebSocket)
[백엔드 서버]
  ↓ 텍스트 누적 저장 (Redis)
  ↓ 세션 종료 신호
[Claude API]
  ↓ 최종 요약
[교사 대시보드] ← 실시간 텍스트 스트림 + 최종 요약
```

#### 기술 스택 변경 사항

| 역할 | Stage 1 | Stage 2 |
|------|---------|---------|
| STT | Whisper (배치) | **Deepgram Nova-3 (스트리밍)** |
| 통신 | HTTP REST | **WebSocket** |
| 임시 저장 | - | **Redis** (실시간 누적) |

#### Deepgram 선택 이유
- 실시간 스트리밍 특화 (지연시간 ~300ms)
- 한국어 지원 (Nova-3 모델)
- Whisper 대비 스트리밍 안정성 우수

---

### Stage 3 — 실시간 양방향 대화 + 요약

AI 선생님이 학생의 말을 듣고 실시간으로 응답한다. 학생은 AI와 대화하며 복습을 진행한다.

#### 아키텍처 옵션 비교

**옵션 A: OpenAI Realtime API** (음성-음성 통합)

```
[학생 브라우저]
  ↔ WebRTC
[OpenAI Realtime API]
  (GPT-4o: STT + LLM + TTS 통합)
  ↓ 대화 완료
[백엔드]
  ↓ 요약 (Claude or GPT-4o)
[교사 대시보드]
```

- 장점: 단일 API, 초저지연(~320ms), 구현 단순
- 단점: GPT-4o 고정, 비용 높음, Claude 미사용

**옵션 B: 파이프라인 조합** (유연성 우선)

```
[학생 마이크]
  ↓ 스트리밍
[Deepgram STT] → 텍스트
  ↓
[Claude API] → 응답 텍스트
  ↓
[ElevenLabs TTS] → 음성
  ↓
[학생 스피커] ← 재생
```

- 장점: Claude 사용, 각 컴포넌트 교체 가능, 비용 최적화
- 단점: 지연시간 합산 (1~2초), 파이프라인 복잡도

**권장: Stage 3 초기에는 옵션 A (빠른 MVP), 이후 옵션 B로 마이그레이션**

#### AI 선생님 시스템 프롬프트 설계

```
[역할]
당신은 학생의 복습을 도와주는 친근한 AI 선생님입니다.
학생이 오늘 학교나 학원에서 배운 내용을 이야기하면,
적절한 질문을 통해 이해도를 확인하고 격려합니다.

[대화 원칙]
- 학생이 먼저 말하도록 유도한다
- 정답을 바로 알려주지 않고 질문으로 생각을 유도한다
- 틀려도 부드럽게 다시 생각해볼 기회를 준다
- 초등~고등 학생 수준에 맞는 언어를 사용한다
- 한 번에 한 가지 질문만 한다

[세션 종료]
대화 종료 시 오늘 복습한 내용을 짧게 요약해서 학생에게 전달한다.
```

---

### Stage 4 — AI 캐릭터 + 생동감 있는 대화

AI 선생님에게 시각적 캐릭터가 부여되어 학생의 몰입도와 친밀감이 높아진다.

#### 추가 컴포넌트

| 역할 | 기술 | 설명 |
|------|------|------|
| 2D 아바타 | **Live2D Cubism** | 일본 애니메이션 스타일, 풍부한 표정 |
| 립싱크 | **OVR LipSync / Rhubarb** | TTS 오디오 → 입모양 동기화 |
| 아바타 렌더링 | **Three.js / Pixi.js** | 브라우저 렌더링 |
| 감정 표현 | LLM 응답에 감정 태그 추가 | 기쁨/놀람/고민 등 표정 전환 |

#### 감정 연동 흐름

```
Claude 응답 텍스트 (감정 태그 포함)
  예: "[기쁨] 정말 잘 이해했네요! 그럼 다음 질문은..."
  ↓
프론트엔드 파싱 → 아바타 표정 변경 + TTS 음성 재생
```

---

## 3. 공통 인프라 설계

### 전체 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    학생 브라우저                          │
│  [녹음/마이크] [아바타 렌더링] [대화 UI] [세션 상태]      │
└────────────────────────┬────────────────────────────────┘
                         │ WebSocket / HTTP
┌────────────────────────▼────────────────────────────────┐
│                  FastAPI 백엔드 서버                      │
│  [세션 관리] [오디오 처리] [대화 오케스트레이션] [요약]   │
└─────┬──────────┬──────────┬──────────┬──────────────────┘
      │          │          │          │
   [Redis]  [PostgreSQL] [S3]   [외부 API]
   (세션)   (영구저장)  (오디오)  (STT/LLM/TTS)

┌─────────────────────────────────────────────────────────┐
│                    교사 대시보드                          │
│  [학생 목록] [실시간 모니터링] [요약 수신] [이력 조회]    │
└─────────────────────────────────────────────────────────┘
```

### 주요 API 엔드포인트

```
POST   /sessions/start          세션 시작 (학생 + 교사 ID)
POST   /sessions/{id}/audio     오디오 파일 업로드 (Stage 1)
WS     /sessions/{id}/stream    실시간 오디오 스트리밍 (Stage 2+)
GET    /sessions/{id}/summary   세션 요약 조회
GET    /teacher/{id}/sessions   교사별 세션 목록
POST   /sessions/{id}/end       세션 종료 + 요약 트리거
```

---

## 4. 비용 예상

### 가정 조건

| 항목 | 값 |
|------|-----|
| 학생 수 | 10명/교사 |
| 세션 횟수 | 20회/월/학생 |
| 월 총 세션 수 | 200세션 |
| AI 대화 시간 | 교사 부재 시 평균 15분/세션 |
| 학생 발화 비율 | 전체의 50% (7.5분 발화) |
| AI 응답 비율 | 전체의 50% (7.5분 응답) |

---

### Stage 1 비용 (Whisper + Claude)

| 항목 | 단가 | 사용량 | 월 비용 |
|------|------|--------|---------|
| Whisper STT | $0.006/min | 15분 × 200 = 3,000분 | **$18** |
| Claude 요약 | $3/1M input, $15/1M output | 입력 ~3,000 tokens, 출력 ~500 tokens | **$2** |
| **합계** | | | **≈ $20/월** |

> 참고: 서버 인프라 비용(AWS EC2/RDS) 별도 (~$30~50/월)

---

### Stage 2 비용 (Deepgram + Claude)

| 항목 | 단가 | 사용량 | 월 비용 |
|------|------|--------|---------|
| Deepgram 스트리밍 | $0.0077/min | 15분 × 200 = 3,000분 | **$23** |
| Claude 요약 | $3/1M input, $15/1M output | Stage 1과 동일 | **$2** |
| **합계** | | | **≈ $25/월** |

---

### Stage 3 비용 — 옵션 A (OpenAI Realtime API)

오디오 1분 ≈ 1,500 audio tokens 기준

| 항목 | 단가 | 사용량 | 월 비용 |
|------|------|--------|---------|
| Realtime 음성 입력 | $32/1M tokens | 7.5분 × 1,500t × 200 = 2.25M tokens | **$72** |
| Realtime 음성 출력 | $64/1M tokens | 7.5분 × 1,500t × 200 = 2.25M tokens | **$144** |
| Realtime 텍스트 처리 | $2.5/1M input, $10/1M output | 소량 | **$5** |
| Claude 최종 요약 | $3/1M input, $15/1M output | Stage 1과 동일 | **$2** |
| **합계** | | | **≈ $223/월** |

---

### Stage 3 비용 — 옵션 B (Deepgram + Claude + ElevenLabs)

| 항목 | 단가 | 사용량 | 월 비용 |
|------|------|--------|---------|
| Deepgram STT | $0.0077/min | 7.5분 × 200 = 1,500분 | **$12** |
| Claude 대화 응답 | $3/1M input, $15/1M output | 입력 50K tokens, 출력 15K tokens/월 | **$0.38** |
| ElevenLabs TTS | 월정액 | Creator 플랜 100K 크레딧 | **$11** |
| Claude 최종 요약 | Stage 1과 동일 | | **$2** |
| **합계** | | | **≈ $25/월** |

> ElevenLabs: AI 응답 텍스트 평균 100자 × 10회 × 200세션 = 200,000자 (Creator 플랜 초과 시 Pro $99/월)

---

### Stage 4 추가 비용 (캐릭터)

| 항목 | 비용 | 비고 |
|------|------|------|
| Live2D 라이선스 | $0 ~ $1,600/년 | 상업용 라이선스 필요 |
| 캐릭터 아트 제작 | 일회성 $500 ~ $3,000 | 외주 또는 직접 제작 |
| 서버 부하 증가 | +$10~20/월 | 렌더링은 클라이언트 사이드 |

---

### 비용 종합 비교표

| 단계 | 기술 | 월 API 비용 | 특징 |
|------|------|------------|------|
| Stage 1 | Whisper + Claude | **~$20** | 가장 저렴, 실시간 아님 |
| Stage 2 | Deepgram + Claude | **~$25** | 실시간 전사, 단방향 |
| Stage 3-A | OpenAI Realtime | **~$223** | 최고 품질, 고비용 |
| Stage 3-B | Deepgram + Claude + ElevenLabs | **~$25** | 양방향, 저비용 |
| Stage 4 | Stage 3-B + 캐릭터 | **~$35** + 초기 개발비 | 최고 몰입도 |

> **권장 경로**: Stage 1 → Stage 2 → Stage 3-B → Stage 4
> Stage 3-A (OpenAI Realtime)는 비용 대비 효과를 검증 후 선택

---

## 5. 기술 리스크 및 대응

| 리스크 | 가능성 | 대응 방안 |
|--------|--------|----------|
| 한국어 STT 인식률 저하 | 중간 | Whisper/Deepgram 비교 테스트, 학생 발음 교정 UI |
| API 응답 지연 (>2초) | 중간 | 스트리밍 응답, 중간 피드백 UI ("생각 중...") |
| 학생 개인정보 보호 | 높음 | 오디오 암호화, 보관 기간 제한, 동의 절차 |
| API 비용 초과 | 낮음 | 세션 시간 제한(max 30분), 비용 모니터링 알림 |
| 학생 이탈 (AI 거부감) | 중간 | 친근한 캐릭터, 선택적 텍스트 모드 병행 |

---

## 6. 구현 로드맵

```
[Week 1-2]  Stage 1 MVP
  - 오디오 녹음 → Whisper → Claude 요약 → 교사 이메일
  - 기본 대시보드 (학생/교사 목록, 요약 조회)

[Week 3-4]  Stage 1 안정화
  - 세션 관리 DB 설계
  - 교사 대시보드 완성
  - 기본 인증 (학생/교사 로그인)

[Week 5-6]  Stage 2
  - Deepgram WebSocket 스트리밍 연동
  - 실시간 텍스트 표시 UI
  - 교사 실시간 모니터링

[Week 7-9]  Stage 3-B
  - 양방향 대화 파이프라인 (Deepgram → Claude → ElevenLabs)
  - AI 선생님 프롬프트 설계 및 튜닝
  - 대화 이력 저장 및 요약

[Week 10-12] Stage 4
  - Live2D 아바타 통합
  - 감정 표현 시스템
  - 사용자 테스트 및 피드백 반영
```

---

## 7. 참고 가격 출처

- [OpenAI API Pricing](https://openai.com/api/pricing/)
- [Deepgram Pricing](https://deepgram.com/pricing)
- [Anthropic Claude Pricing](https://platform.claude.com/docs/en/about-claude/pricing)
- [ElevenLabs API Pricing](https://elevenlabs.io/pricing/api)
- [OpenAI Whisper Pricing](https://diyai.io/ai-tools/speech-to-text/openai-whisper-api-pricing-2026/)
